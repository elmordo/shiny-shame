<?php
// pokud je uzivatel opravnen, zobrazi se odkaz pro vytvoreni experimentu
$user = Zend_Auth::getInstance()->getIdentity();
$acl = Zend_Controller_Front::getInstance()->getPlugin("MP_Controller_Plugin_Acl")->getAcl();

if ($acl->isAllowed($user, "experiment", "post")) {
    // zobrazeni odkazu na vytvoreni noveho experimentu
    $url = $this->url(array(), "create-experiment");
    
    printf("<a href='%s'>New experiment</a>", $url);
}

echo $this->experiment()->table($this->experiments);
?>