<?php
if ($this->isDeleted) {
    $url = $this->url($this->serieInfo, "sample-get");
    $redirector = new Zend_Controller_Action_Helper_Redirector();
    $redirector->gotoUrlAndExit($url);
}

echo $this->header("Delete serie");
echo $this->form;
?>
